# Enable hoisting for better compatibility with tools
node-linker=hoisted

# Auto-install peers
auto-install-peers=true

# Strict peer dependencies (fail on missing peers)
strict-peer-dependencies=false
